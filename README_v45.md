# CMS v45 — Admin Fixed

- Sửa JavaScript bị in thành text ở cuối trang.
- Login helper được chuyển ra đúng cuối file HTML.
- Sửa literal \n làm hỏng cú pháp trong saveArticle().
- Main inline JavaScript: node --check PASS.
- Login binder JavaScript: node --check PASS.
- Giữ Structured CMS và layout-preserving renderer.
